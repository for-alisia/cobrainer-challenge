/** Components */

/** Styles */
import styles from './DocList.module.scss';

const DocList = () => {
  return (
    <div className={styles.wrapper}>
      <span>List of Docs go here</span>
    </div>
  );
};

export default DocList;
